name = "wc26-football-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"
workers_dev = true

# Optional: lock CORS to your site's origin instead of allowing any (*).
# Uncomment and set to your GitHub Pages origin:
# [vars]
# ALLOW_ORIGIN = "https://oscarzu.github.io"

# The API-Football key is a SECRET, not a var. Set it with:
#   npx wrangler secret put API_FOOTBALL_KEY
